# Core scientific Python stack
pandas>=2.0
numpy>=1.24
scipy>=1.11
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# LLM classification (Notebook 02c, 02e)
anthropic>=0.34
python-dotenv>=1.0

# Latent class analysis (Notebook 04)
stepmix>=2.0

# Utilities
tqdm>=4.65
requests>=2.31

# Optional — only needed if re-running the cross-classified RE analysis
# with the R-based lmer engine rather than the statsmodels fallback:
# pymer4>=0.8    # requires R + lme4 installed on the system
